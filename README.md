# Super MultiBot All-in-One

Start command:

```bash
python -m app.main
```

Required `.env`:

```env
BOT_TOKEN=GLAVNI_BOT_TOKEN
SUPER_ADMIN_ID=5907118746
DATABASE_PATH=data.db
```

Foydalanuvchi BotFatherdan token olib glavni botga yuboradi. To'lov tasdiqlanmaguncha child bot ishlamaydi. Super admin hamma botlarni yoqadi/o'chiradi.
